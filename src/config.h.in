#pragma once

// NOLINTBEGIN(modernize-macro-to-enum,cppcoreguidelines-macro-usage)

/* If we are in debug mode.
 *
 * Why is this a function macro?
 * It prevents silent undefined macro errors.
 *
 * https://stackoverflow.com/a/3160643
 */
#cmakedefine01 DEBUG()

// Version info
/* clang-format off */
/* NOLINTBEGIN */
#define VERSION_MAJOR @krompir_VERSION_MAJOR@
#define VERSION_MINOR @krompir_VERSION_MINOR@
#define VERSION_PATCH @krompir_VERSION_PATCH@

#define VERSION "@krompir_VERSION@"
/* clang-format on */
/* NOLINTEND */

#define BINLOG_VERSION "4cd8f78"

// App names
#ifdef NDEBUG
#  define APP_NAME "Krompir"
#else
#  define APP_NAME "kROMPIR"
#endif

// NOLINTEND(modernize-macro-to-enum,cppcoreguidelines-macro-usage)
